<?xml version="1.0" encoding="utf-8"?>
<ripple xmlns:android="http://schemas.android.com/apk/res/android"
    android:color="@color/pinkY"> <!-- Ripple effect color -->

    <!-- Button states -->
    <item>
        <selector>
            <!-- Disabled state -->
            <item android:state_enabled="false">
                <shape android:shape="rectangle">
                    <corners android:radius="10dp" />
                    <stroke android:width="3dp" android:color="@color/grayY" />
                    <solid android:color="@color/grayX"/>
                </shape>
            </item>
            <!-- Default state -->
            <item>
                <shape android:shape="rectangle">
                    <corners android:radius="10dp" />
                    <stroke android:width="3dp" android:color="@color/pinkY" />
                    <solid android:color="@color/pinkX"/>
                </shape>
            </item>
        </selector>
    </item>

</ripple>
