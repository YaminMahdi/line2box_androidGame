<?xml version="1.0" encoding="utf-8"?>
<ripple xmlns:android="http://schemas.android.com/apk/res/android"
    android:color="@color/grayY"> <!-- Ripple effect color -->

    <!-- Default button shape states -->
    <item>
        <selector>
            <!-- Default state -->
            <item>
                <shape android:shape="rectangle">
                    <corners android:radius="@dimen/_18sdp" />
                    <stroke android:width="@dimen/_2sdp" android:color="@color/grayY" />
                    <solid android:color="@color/grayX"/>
                </shape>
            </item>
        </selector>
    </item>

</ripple>
