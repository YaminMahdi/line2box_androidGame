<?xml version="1.0" encoding="utf-8"?>
<ripple xmlns:android="http://schemas.android.com/apk/res/android"
    android:color="@color/greenY"> <!-- Ripple effect color -->

    <!-- Default and disabled states -->
    <item>
        <selector>
            <!-- Disabled state -->
            <item android:state_enabled="false">
                <shape android:shape="rectangle">
                    <corners android:radius="15dp" />
                    <stroke android:width="3dp" android:color="@color/grayY" />
                    <solid android:color="@color/grayX"/>
                </shape>
            </item>
            <item>
                <shape android:shape="rectangle">
                    <corners android:radius="15dp" />
                    <stroke android:width="3dp" android:color="@color/greenY" />
                    <solid android:color="@color/greenX"/>
                </shape>
            </item>
        </selector>
    </item>

</ripple>
